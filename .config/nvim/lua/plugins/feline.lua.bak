-- Disable statusline and load feline-nvim
 return {
  "freddiehaddad/feline.nvim",
  event = "VeryLazy",
  opts = {},
  specs = {
    {
      "rebelot/heirline.nvim",
      optional = true,
      config = function(_, opts)
        opts.statusline = nil
        require('feline').use_theme()          -- to use a custom theme
      end
    },
  },
}
